name: synx
version: 3.6.3
description: Native Dart parser for SYNX (Active Data Format). Parity with synx-core 3.6.x. No FFI — pure Dart.
homepage: https://synx.aperturesyndicate.com
repository: https://github.com/aperturesyndicate/synx

environment:
  sdk: ^3.0.0

dev_dependencies:
  test: ^1.24.0
